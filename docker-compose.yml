services:
  homelab-showcase:
    build: .
    container_name: homelab-showcase
    restart: unless-stopped
    ports:
      - "9434:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
